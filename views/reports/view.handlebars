<style>
  .fa-star {
    color: orange;
  }
</style>
<div class="col-md-12">
  <div class="card">
    <div class="header">
      <div class="row">
        <div class="col-md-12">
          <h2 class="title ">Relatórios</h2>
        </div>
      </div>
    </div>
    <div class="content">
      <div class="row">
        <div class="col-md-5">
          <div class="form-group">
            <label for="">Congresso</label>
            <select class="form-control" name="congress">
              <option value="">Selecione</option>
              {{#each dataCongress}}
              <option {{#if this.select}} selected {{/if}} value="{{this.cod}}">{{this.name}}</option>
              {{/each}}
            </select>
          </div>
        </div>
        <div class="col-md-5">
          <div class="form-group">
            <label for="">Evento</label>
            <select class="form-control" name="event">
              <option value="">Selecione</option>
            </select>
          </div>
        </div>
        <div class="col-md-2">
          <label for=""></label>
          <button style="margin-top: 3px;" type="button" id="buscar"
            class="btn btn-fill btn-info btn-block">Buscar</button>
        </div>
      </div>
      {{#if dataReports}}
      {{> details_reports}}
      {{/if}}
    </div>
  </div>
</div>

<script>
  let eventos =  [
  {{#each dataEvent}}
   {
    'cod' : {{this.cod}},
    'nome':  '{{this.name}}',
    'congresso':  {{this.CongressId}},
  },
  {{/each}}
  ]
  let select = {{eventSelect}}
</script>

<script src="/js/page/relatorios.js" type="text/javascript"></script>