
<div class="col-md-6 col-md-push-3">
  <div class="card">
    <div class="header">
      <div class="row">
        <div class="col-md-12 text-center">
          <h2 class="title ">Recuperar Senha</h2>
          <h4 class="title ">Insira a nova senha</h4>
        </div>
      </div>
    </div>
    <div class="content">
      <form action="/nova-senha" method="post">
        <input type="hidden" name="id" class="form-control" value="{{id}}" >
        <input type="hidden" name="code" class="form-control" value="{{code}}" >
        <div class="row">
          <div class="col-md-12">
            <div class="form-group">
              <label>Senha</label>
              <input required type="password" minlength="8" name="password" onChange="onChangePass()" class="form-control" placeholder="Senha" value="">
            </div>
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
            <div class="form-group">
              <label>Confirme sua senha</label>
              <input required type="password" minlength="8" name="confirmPassword" onChange="onChangePass()" class="form-control" placeholder="Senha" value="">
            </div>
          </div>
        </div>
        <div class="row">
          <div class="col-xs-6 col-sm-3">
            <a target="" href="/login"><button type="button" class="btn btn-default btn-block">Voltar</button></a>
          </div>
          <div class="col-xs-6 col-sm-3 col-md-push-6">
            <button type="submit" class="btn btn-info btn-fill btn-block">Alterar </button>
          </div>
        </div>
        <div class="clearfix"></div>
      </form>
    </div>
  </div>
</div>

<script>
  function onChangePass() {
    const password = document.querySelector('input[name=password]');
    const confirm = document.querySelector('input[name=confirmPassword]');
    if (confirm.value === password.value) {
      confirm.setCustomValidity('');
    } else {
      confirm.setCustomValidity('Senhas não conferem');
    }
  }
</script>